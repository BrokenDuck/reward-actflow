# Active Pre-Training for Generative Discovery

This project makes extensive use of [flowgym](https://github.com/cristianpjensen/flowgym). 

Example usage:
```bash
python -m active_pretraining.run --problem_setup toy --dir experiments/toy

python -m active_pretraining.run --problem_setup mnist --dir experiments/mnist
```
